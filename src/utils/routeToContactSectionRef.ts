import Router from 'next/router'
import { useContext } from 'react'
import { Context } from 'src/stores/Context'

function RouteToContactSectionRef() {
  console.log('push')
  // context?.contactRef
}

export default RouteToContactSectionRef
